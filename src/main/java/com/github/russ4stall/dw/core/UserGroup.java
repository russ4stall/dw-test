package com.github.russ4stall.dw.core;

import java.util.HashMap;

/**
 * A map that represents the groups a user is in.
 * Maps a groupId(Integer) to their role in the group (String(should really be an enum)).
 * @author Russ Forstall
 */
public class UserGroup extends HashMap<Integer, String> {

}
